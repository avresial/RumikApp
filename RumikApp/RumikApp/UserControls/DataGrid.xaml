<UserControl x:Class="RumikApp.UserControls.DataGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RumikApp.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name = "dataGrid" AlternatingRowBackground = "LightBlue"  
         AlternationCount = "2" AutoGenerateColumns = "False">

            <DataGrid.Columns>
                <DataGridTextColumn Header = "Nazwa" Binding = "{Binding Name}" />
                <DataGridTextColumn Header = "Pojemność" Binding = "{Binding Capacity}" />
                <DataGridTextColumn Header = "Cena" Binding = "{Binding Price}"/>
                <DataGridTextColumn Header = "Smak" Binding = "{Binding Taste}"/>
                <DataGridTextColumn Header = "Smak z colą" Binding = "{Binding TasteC}"/>
                <DataGridComboBoxColumn Header = "Nuty smakowe" SelectedItemBinding = "{Binding FlavorNotes}"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
